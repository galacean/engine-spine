self.addEventListener("message", (e) => {
  console.log(e);
  decodeImage(e.data);
});
function decodeImage(ab) {
  const blob = new Blob([ab]);
  createImageBitmap(blob).then((bitmap) => {
    self.postMessage(bitmap, [bitmap]);
  });
}
