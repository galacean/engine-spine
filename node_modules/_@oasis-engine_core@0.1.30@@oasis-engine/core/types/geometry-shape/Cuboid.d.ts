import { Engine } from "../Engine";
import { GeometryShape } from "./GeometryShape";
/**
 * 长方体
 */
export declare class CuboidGeometry extends GeometryShape {
    /**
     * 创建长方体。
     * @param width - 宽
     * @param height - 高
     * @param depth - 深
     * @param engine - 引擎
     */
    constructor(width?: number, height?: number, depth?: number, engine?: Engine);
}
