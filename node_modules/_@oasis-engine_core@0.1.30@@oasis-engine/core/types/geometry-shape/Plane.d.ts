import { Engine } from "../Engine";
import { GeometryShape } from "./GeometryShape";
/**
 * CubeGeometry 平面创建类
 * @extends BufferGeometry
 */
export declare class PlaneGeometry extends GeometryShape {
    private _parameters;
    private halfWidth;
    private halfHeight;
    /**
     * @constructor
     * @param {number} width 宽
     * @param {number} height 高
     * @param {number} horizontalSegments 水平分段数
     * @param {number} verticalSegments 垂直分段数
     */
    constructor(width?: number, height?: number, horizontalSegments?: number, verticalSegments?: number, engine?: Engine);
    initialize(engine: Engine): void;
}
