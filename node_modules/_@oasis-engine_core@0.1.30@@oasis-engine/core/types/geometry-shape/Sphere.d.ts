import { Engine } from "../Engine";
import { GeometryShape } from "./GeometryShape";
/**
 * SphereGeometry 球体创建类
 * @extends BufferGeometry
 */
export declare class SphereGeometry extends GeometryShape {
    private _parameters;
    private _thetaEnd;
    /**
     * @constructor
     * @param {number} radius 半径
     * @param {number} horizontalSegments 水平分段数
     * @param {number} verticalSegments 垂直分段数
     * @param {number} alphaStart 水平开始角度
     * @param {number} alphaRange 水平角度范围
     * @param {number} thetaStart 垂直开始角度
     * @param {number} thetaRange 垂直角度范围
     */
    constructor(radius?: number, horizontalSegments?: number, verticalSegments?: number, alphaStart?: number, alphaRange?: number, thetaStart?: number, thetaRange?: number, engine?: Engine);
    /**
     * 构造球体数据
     * @private
     */
    initialize(engine: Engine): void;
}
