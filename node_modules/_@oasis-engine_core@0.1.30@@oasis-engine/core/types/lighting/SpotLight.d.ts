import { Vector3 } from "@oasis-engine/math";
import { Light } from "./Light";
/**
 * 聚光灯创建类
 * @extends Light
 */
export declare class SpotLight extends Light {
    private _forward;
    private _lightColor;
    private _inverseDirection;
    color: Vector3;
    penumbra: number;
    distance: number;
    intensity: number;
    decay: number;
    angle: number;
    /**
     * @constructor
     * @param {Entity} entity 节点对象
     */
    constructor(entity: any);
    /** 获取聚光灯位置
     * @return {Vector3} 位置坐标
     * @readonly
     */
    get position(): Vector3;
    /** 获取聚光灯方向
     * @return {Vector3} 方向向量
     * @readonly
     */
    get direction(): Vector3;
    /** 获取聚光灯方向的反方向
     * @return {Vector3} 方向向量
     * @readonly
     */
    get reverseDirection(): Vector3;
    /** 最终光照颜色
     * @return {Vector3} 最终光照颜色
     * @readonly
     */
    get lightColor(): Vector3;
    /**
     * 生成 Technique 所需的 uniform 定义
     * @param {string} uniformName
     */
    static getUniformDefine(uniformName: any): {};
    /**
     * 将灯光参数值提交到材质对象
     */
    bindMaterialValues(mtl: any, uniformName: any): void;
}
