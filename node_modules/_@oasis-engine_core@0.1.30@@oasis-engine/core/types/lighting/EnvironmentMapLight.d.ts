import { Vector3 } from "@oasis-engine/math";
import { Light } from "./Light";
/**
 * 环境光源
 */
export declare class EnvironmentMapLight extends Light {
    diffuseMap: any;
    specularMap: any;
    diffuse: Vector3;
    specular: Vector3;
    diffuseIntensity: any;
    specularIntensity: any;
    /**
     * 生成 Technique 所需的 uniform 定义
     * @param {string} uniformName
     */
    static getUniformDefine(uniformName: any): {};
    /**
     * 环境光源
     * @param {Entity} entity 节点对象
     */
    constructor(entity: any);
    /**
     * 是否使用diffuse贴图
     * @private
     * @returns {Boolean}
     */
    get useDiffuseMap(): boolean;
    /**
     * 是否使用Specular贴图
     * @private
     * @returns {Boolean}
     */
    get useSpecularMap(): boolean;
    /**
     * 将灯光参数绑定到指定的材质对象上
     */
    bindMaterialValues(mtl: any, uniformName: any): void;
}
