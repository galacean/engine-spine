import { Vector3 } from "@oasis-engine/math";
import { Entity } from "../Entity";
import { Probe } from "./Probe";
import { CubeProbeConfig } from "./type";
/**
 * 立方体探针,生成 cubeTexture,用于 动态环境反射 等效果
 * */
export declare class CubeProbe extends Probe {
    /** 可以设置探针的位置，默认为原点 [0,0,0] */
    position: Vector3;
    private oriViewMatrix;
    private oriInverseViewMatrix;
    private oriProjectionMatrix;
    private oriInverseProjectionMatrix;
    /**
     * 创建探针
     * @param {Entity} node
     * */
    constructor(node: Entity);
    /**
     * 初始化探针。
     * @param config - 初始化配置
     */
    init(config?: CubeProbeConfig): void;
    /**
     * 贮藏原相机参数
     * */
    private storeCamera;
    /**
     * 还原相机参数
     * */
    private restoreCamera;
    protected preRender(): void;
    protected render(): void;
    protected postRender(): void;
    /**
     * 根据渲染面设置相机参数
     * */
    private setCamera;
}
