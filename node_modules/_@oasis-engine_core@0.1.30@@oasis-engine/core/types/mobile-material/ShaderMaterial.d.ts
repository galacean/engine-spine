import { DataType, RenderState, UniformSemantic } from "../base/Constant";
import { Engine } from "../Engine";
import { Material } from "../material/Material";
import { RenderTechnique } from "../material/RenderTechnique";
import { TechniqueStates } from "../material/type";
export declare class ShaderMaterial extends Material {
    vertexShader: string;
    fragmentShader: string;
    isValid: boolean;
    attributes: {};
    private _uniforms;
    private _renderStates;
    private _enableConfig;
    private _disableConfig;
    private _functionsConfig;
    constructor(engine: Engine, name: any);
    prepareDrawing(context: any, component: any, primitive: any): void;
    updateTechnique(): void;
    _generateTechnique(camera: any, component: any, primitive: any): RenderTechnique;
    addState(key: string, state: Array<any>): void;
    removeState(key: string, state: any): void;
    get renderStates(): TechniqueStates;
    set renderStates(value: TechniqueStates);
    get uniforms(): {
        matModelViewProjection: {
            name: string;
            semantic: UniformSemantic;
            type: DataType;
        };
        matModelView: {
            name: string;
            semantic: UniformSemantic;
            type: DataType;
        };
    };
    set uniforms(value: {
        matModelViewProjection: {
            name: string;
            semantic: UniformSemantic;
            type: DataType;
        };
        matModelView: {
            name: string;
            semantic: UniformSemantic;
            type: DataType;
        };
    });
    set blend(value: boolean);
    set blendSrcFactor(value: string);
    set blendDstFactor(value: string);
    set doubleSide(value: boolean);
    set depthTest(value: boolean);
    static commonUniforms: {
        matModelViewProjection: {
            name: string;
            semantic: UniformSemantic;
            type: DataType;
        };
        matModelView: {
            name: string;
            semantic: UniformSemantic;
            type: DataType;
        };
    };
    static commonEnable: RenderState[];
    static commonDisable: RenderState[];
}
export declare function union(arr1: Array<any>, arr2: Array<any>): Array<any>;
