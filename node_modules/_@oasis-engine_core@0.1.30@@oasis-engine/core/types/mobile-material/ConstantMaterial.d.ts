import { CommonMaterial } from "./CommonMaterial";
/**
 * 显示固定颜色（不计算光照）的材质
 * color = <emission> + <ambient> * al
 */
export declare class ConstantMaterial extends CommonMaterial {
    /**
     * 生成内部所使用的 Technique 对象
     * @private
     */
    _generateTechnique(): void;
}
