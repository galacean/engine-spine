/**
 * Transparent mode.
 */
export declare enum AlphaMode {
    /** Opaque mode. */
    Opaque = 0,
    /** Color blending mode. */
    Blend = 1,
    /** Transparency cutoff mode. */
    CutOff = 2
}
