import { RenderBufferDepthFormat } from "../base/Constant";
import { Engine } from "../Engine";
import { Texture } from "./Texture";
/**
 * 类应用于渲染深度纹理。
 */
export declare class RenderDepthTexture extends Texture {
    private _format;
    private _autoMipmap;
    /**
     * 格式。
     */
    get format(): RenderBufferDepthFormat;
    /**
     * 是否自动生成多级纹理。
     */
    get autoGenerateMipmaps(): boolean;
    set autoGenerateMipmaps(value: boolean);
    /**
     * 构造渲染深度纹理。
     * @param width - 宽
     * @param height - 高
     * @param format - 格式。默认 RenderBufferDepthFormat.Depth,深度纹理,自动选择精度
     * @param mipmap - 是否使用多级纹理
     * @param isCube - 是否为立方体模式
     * @param engine - 可选引擎
     */
    constructor(width: number, height: number, format?: RenderBufferDepthFormat, mipmap?: boolean, isCube?: boolean, engine?: Engine);
}
