/**
 * 一组GL渲染状态控制，对应glTF里面的“technique.states”对象
 * @class
 * @private
 */
declare class RenderStateBlock {
    private _enable;
    private _disable;
    private _functions;
    constructor();
    /**
     * 使用glTF 1.0格式的数据创建
     * @param {object} cfg glTF格式的数据
     */
    createFromGLTF(cfg: any): void;
    /**
     * 将状态设置到GL/RenderStateManager
     * @param {RenderStateManager} stateManager
     */
    apply(stateManager: any): void;
}
export default RenderStateBlock;
