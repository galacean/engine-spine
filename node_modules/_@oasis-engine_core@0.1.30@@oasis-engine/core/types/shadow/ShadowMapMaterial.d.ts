import { DataType } from "../base/Constant";
import { ComplexMaterial } from "../material/ComplexMaterial";
import { RenderTechnique } from "../material/RenderTechnique";
/**
 * 生成 Shadow Map 使用的材质
 * @private
 */
export declare class ShadowMapMaterial extends ComplexMaterial {
    /**
     * 生成内部所使用的 Technique 对象
     * @private
     */
    _generateTechnique(camera: any, component: any): RenderTechnique;
    /**
     * 添加阴影相关的 uniform 定义
     * @private
     */
    _generateFragmentUniform(): {
        u_viewMatFromLight: {
            name: string;
            type: DataType;
        };
        u_projMatFromLight: {
            name: string;
            type: DataType;
        };
    };
    /**
     * 根据阴影的个数，添加相应的宏定义
     * @private
     */
    _generateMacros(): any[];
}
