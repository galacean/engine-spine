import { Matrix } from "@oasis-engine/math";
import { CompFunc, DataType, RenderState } from "../base/Constant";
import { Engine } from "../Engine";
import { Material } from "../material/Material";
/**
 * 天空盒材质
 */
export declare class SkyBoxMaterial extends Material {
    private _cacheMat1;
    private _cacheMat2;
    private modelMatrix;
    constructor(engine: Engine, name?: string);
    setModel(modelMatrix: Matrix): void;
    /**
     * 渲染前调用
     * @private
     */
    prepareDrawing(context: any, component: any): void;
    /**
     * 创建Technique
     * @private
     */
    _generateTechnique(): void;
    static defaultName: string;
    static techniqueName: string;
    static vertexShader: string;
    static fragmentShader: string;
    static techniqueConfig: {
        attributes: {};
        uniforms: {
            u_mvpNoscale: {
                name: string;
                type: DataType;
            };
            u_cube: {
                name: string;
                type: DataType;
            };
        };
        states: {
            disable: RenderState[];
            functions: {
                depthFunc: CompFunc;
            };
        };
    };
}
