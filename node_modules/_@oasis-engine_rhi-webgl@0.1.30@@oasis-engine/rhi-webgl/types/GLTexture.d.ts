import { GLAsset } from "./GLAsset";
import { WebGLRenderer } from "./WebGLRenderer";
import { Texture } from "@oasis-engine/core";
/**
 * 管理贴图对象
 * @class
 * @private
 */
export declare class GLTexture extends GLAsset {
    protected _gl: any;
    private _glTexture;
    protected _config: Texture;
    protected _type: any;
    constructor(rhi: WebGLRenderer, config: Texture, type: any);
    /**
     * 内部的WebGLTexture对象
     * @readonly
     * @private
     */
    get glTexture(): WebGLTexture;
    /**
     * 绑定到指定的 TEXTURE UNIT
     * @private
     */
    activeBinding(textureIndex: any): void;
    /**
     * 释放 GL 资源
     * @private
     */
    finalize(): void;
}
