import { AssetObject } from "@oasis-engine/core";
import { WebGLRenderer } from "./WebGLRenderer";
import { GLAsset } from "./GLAsset";
/**
 * 引擎的资源对象所需要的 GL 资源对象的 Cache 管理
 * @private
 */
export declare class GLAssetsCache {
    private readonly _rhi;
    private _objectSet;
    private _checkList;
    private _nextID;
    private readonly _enableCollect;
    constructor(rhi: WebGLRenderer, props?: any);
    /**
     * 为一个引擎资源对象创建对应的 GL 资源对象
     * @param {object} asset
     * @param {class} ctor
     */
    requireObject(asset: AssetObject, ctor: new (...args: any) => GLAsset): any;
    /**
     * 清除 Cache 中没有用到的 GL 资源对象
     */
    compact(): void;
    /**
     * 释放内部登记的所有对象
     */
    finalize(): void;
}
