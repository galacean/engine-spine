import { WebGLRenderer } from "./WebGLRenderer";
import { GLTexture } from "./GLTexture";
/**
 * GL CubeMap 资源管理
 * @private
 */
export declare class GLTextureCubeMap extends GLTexture {
    constructor(rhi: WebGLRenderer, config: any);
    /**
     * 绑定到指定的texture index
     * @private
     */
    activeBinding(textureIndex: any): void;
}
