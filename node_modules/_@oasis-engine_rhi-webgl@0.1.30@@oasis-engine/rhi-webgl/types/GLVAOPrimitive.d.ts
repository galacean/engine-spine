import { Primitive } from "@alipay/o3-core";
import { GLPrimitive } from "./GLPrimitive";
import { GLTechnique } from "./GLTechnique";
import { WebGLRenderer } from "./WebGLRenderer";
/**
 * 基于 VAO 的 GLPrimitive
 * */
export declare class GLVAOPrimitive extends GLPrimitive {
    private vao;
    constructor(rhi: WebGLRenderer, primitive: Primitive);
    /** 注册 VAO */
    private registerVAO;
    /**
     * 使用 VAO 执行绘制操作
     * @param {GLTechnique} tech
     */
    draw(tech: GLTechnique): void;
    /**
     * 释放 GL 资源
     */
    finalize(): void;
}
