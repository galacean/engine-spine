import { Oasis } from "../Oasis";
import { AssetConfig } from "../types";
import { SchemaResource } from "./SchemaResource";
export declare class ShaderMaterialResource extends SchemaResource {
    private scripts;
    private loadShaderDefine;
    private createMaterial;
    load(resourceLoader: any, assetConfig: AssetConfig, oasis: Oasis): Promise<ShaderMaterialResource>;
    setMeta(assetConfig?: AssetConfig): void;
    updateMeta(key: string, value: any): void;
    update(key: string, value: any): void;
}
