'use strict';

const parser = require('./nunjucks-no-chokidar').parser;
const Require = require('./tag-require');

module.exports = {
  parse: function(src, extensions, lexerTags) {
    return parser.parse.call(this, src, [new Require()].concat(extensions), lexerTags);
  },
};
